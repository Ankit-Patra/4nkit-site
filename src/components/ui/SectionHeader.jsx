const SectionHeader = ({ title }) => (
    <div className="section-header">
        <h2 className="subtle-glitch">
            <span className="prompt">{">"}</span> {title}
        </h2>
    </div>
);
export default SectionHeader;
